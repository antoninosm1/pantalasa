<?xml version="1.0" encoding="utf-8"?>
<!-- C:\xampp\htdocs\pantalasa\android_sdhybc\app\src\main\AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pantalasa.sdhybc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:icon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sdhybc"
        tools:targetApi="31">
        <!-- Actividades principales - CORREGIDO EL ORDEN -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LineActivity"
            android:exported="false" />

        <activity
            android:name=".OnlineLoginActivity"
            android:exported="false" />

        <activity
            android:name=".RepositoriosActivity"
            android:exported="false" />

        <activity
            android:name=".OnlineCedulasGradillaActivity"
            android:exported="false" />

    </application>
</manifest>
